const PageLoader = () => (
  <div className="min-h-screen pt-24 flex items-center justify-center">
    <p className="text-muted-foreground font-body text-lg animate-pulse">Loading...</p>
  </div>
);

export default PageLoader;
